{
  "name": "shinkan.today",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "npm run standard && npm run mocha",
    "build": "npm run rsync && npm run rollup-main && npm run rollup-book && npm run postcss",
    "start": "npm run watch & npm run server",
    "deploy": "npm run build && npm run update && npm run gh-pages",
    "deploy-from-travis": "npm run build && npm run update && npm run gh-pages-travis",
    "standard": "standard *.js lib/*.js src/*.js",
    "mocha": "mocha --require reify test/",
    "rsync": "rsync -avu --exclude '*.tag' --exclude '*.js' --exclude '*.css' --exclude '*.sketch' src/ dist/",
    "rollup-main": "rollup -c -i src/main.js -o dist/main.js",
    "rollup-book": "rollup -c -i src/book.js -o dist/book.js",
    "postcss": "postcss -u postcss-cssnext -u postcss-import -d dist/ src/global.css src/book.css",
    "watch": "chokidar 'src/**' -c 'npm run build'",
    "server": "browser-sync start --files 'dist/main.js' --server dist",
    "update": "node --require reify update",
    "gh-pages": "gh-pages -d dist",
    "gh-pages-travis": "gh-pages -d dist -r \"https://$GIT_TOKEN@github.com/cognitom/shinkan.today.git\""
  },
  "dependencies": {
    "fecha": "^2.3.0",
    "normalize-css": "^2.3.1",
    "riot": "^3.1.0",
    "riot-route": "^3.1.0",
    "whatwg-fetch": "^2.0.2"
  },
  "devDependencies": {
    "JSONStream": "^1.3.0",
    "browser-sync": "^2.18.2",
    "chokidar-cli": "^1.2.0",
    "del": "^2.2.2",
    "fs-promise": "^2.0.0",
    "gh-pages": "^0.12.0",
    "highland": "^2.10.1",
    "js-yaml": "^3.7.0",
    "lodash.get": "^4.4.2",
    "mocha": "^3.2.0",
    "moment": "^2.17.1",
    "postcss": "^5.2.6",
    "postcss-cli": "^2.6.0",
    "postcss-cssnext": "^2.9.0",
    "postcss-import": "^9.1.0",
    "reify": "^0.4.4",
    "request": "^2.79.0",
    "rollup": "^0.41.4",
    "rollup-plugin-buble": "^0.15.0",
    "rollup-plugin-commonjs": "^7.0.0",
    "rollup-plugin-node-resolve": "^2.0.0",
    "rollup-plugin-riot": "^1.0.0",
    "standard": "^8.6.0"
  }
}
